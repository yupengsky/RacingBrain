#!/usr/bin/env bash
set -euo pipefail

# TODO: 在这里实现 ROS apt 依赖的本地提取逻辑。
# 典型用途：
# - 下载 vision_msgs 等 ROS 官方包
# - 解包到仓库本地前缀
# - 让后续 build / run 能通过环境变量找到它们

echo "[TODO] install_ros_apt_deps_local.sh"
echo "在这里本地安装缺失的 ROS 依赖包。"
