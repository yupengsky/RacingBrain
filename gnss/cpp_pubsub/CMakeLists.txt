cmake_minimum_required(VERSION 3.8)
project(cpp_pubsub)

find_package(ament_cmake REQUIRED)

# TODO:
# 1. find_package(rclcpp REQUIRED)
# 2. find_package(gnss_ins_msg REQUIRED)
# 3. add_executable(...) / target_link_libraries(...)
# 4. install(TARGETS ...)

ament_package()
